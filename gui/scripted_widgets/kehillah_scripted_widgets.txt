# =====================================================================
# Kehillah scripted-widget registrations.
#
# This is CK3's one additive GUI mechanism -- the only way found, across
# two research passes in this repo (docs/spec/gui-spike-community-list.md,
# docs/spec/spike-domicile-map-visibility.md), to put a new element on
# screen without forking a vanilla .gui file. The engine's own
# documentation for it is game/gui/scripted_widgets/_scripted_widgets.info:
#
#   "Files here can contain pairs of file path and widget names to
#    automatically create widgets on startup that are not formally
#    referenced by the code."
#   "All files will be loaded so multiple mods can load their own widgets
#    as long as they don't mess with each other's pathing."
#
# -- i.e. this file MERGES with other mods' registrations rather than
# overwriting them, which is exactly the property the two spikes went
# looking for and could not find anywhere else. Vanilla ships the folder
# with only the .info file in it, so nothing is being overridden here.
#
# Entries: the Jewish Communities map view (three widgets) and the
# community library panel (three windows).
# =====================================================================

gui/kehillah_community_map_view.gui = kehillah_community_map_view

# The roster window is a separate top-level widget from the toggle (see
# the .gui file's header for why: a movable window cannot live inside
# the flowcontainer that lays out the button).
gui/kehillah_community_map_view.gui = kehillah_community_map_view_panel

# Your own community's scores, always on screen under the resource bar
# (v12 section 10).
gui/kehillah_community_map_view.gui = kehillah_own_community_strip

# The community library panel: what works a Kehillah's Beit Midrash
# holds, shown under the vanilla domicile window whenever it is open on
# a Kehillah's quarter (docs/spec/v13-domicile-library-panel.md).
gui/kehillah_domicile_library.gui = kehillah_domicile_library
# Its twin for a quarter opened from the map roster (same file header
# for why these are two windows and not one).
gui/kehillah_domicile_library.gui = kehillah_domicile_library_other
# And the standalone one the roster's library button opens beside the
# roster, no domicile view needed (2026-09-19).
gui/kehillah_domicile_library.gui = kehillah_domicile_library_standalone
