# =====================================================================
# Decision group for Kehillah community decisions.
#
# Given its own group so the community's handful of decisions do not get
# buried among the dozens of generic ones a character can normally see.
# Sorted just below vanilla's major decisions.
# =====================================================================

kehillah = {
	sort_order = 240
	gui_tags = { big_button }
}
