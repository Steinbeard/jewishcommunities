# =====================================================================
# Kehillah officers -- Chief Rabbi, Treasurer, Shtadlan.
#
# Source of truth: docs/spec/v1-kehillah-community.md sections 3 and 3c.
#
# WHY COURT POSITIONS RATHER THAN A COUNCIL. The Kehillah government
# sets council = no: a landless ruler has no counties for councillor
# tasks to act on, and vanilla's own landless government disables the
# council for the same reason. Court positions carry no such assumption
# -- vanilla's camp officers are court positions on a landless
# government, and this file follows that precedent closely.
#
# THE GATING RULE (spec section 3c): "Characters gate on buildings, not
# just currency. You cannot appoint a Chief Rabbi without a Beit
# Midrash, no Treasurer without a Countinghouse, no Shtadlan without the
# Chambers." Each valid_position block below enforces exactly that by
# checking the domicile parameter its building sets. This is what makes
# growing the quarter and populating it with people reinforce each
# other instead of being two separate tracks.
#
# These three are also the open half of the succession candidate pool --
# see common/succession_appointment/kehillah_leadership.txt. Appointing
# a capable Shtadlan is therefore not only a present-tense decision; it
# is choosing who you may be playing as after the current leader dies.
#
# NOTE: officers get a real Gold salary. A community pays its rabbi and
# its treasurer; that expense is a deliberate counterweight to the
# Prosper pillar rather than a free bonus.
# =====================================================================

@kehillah_officer_will_do = 20


# =====================================================================
# CHIEF RABBI -- Learning. Gated on the Beit Midrash.
# =====================================================================

chief_rabbi_kehillah_position = {
	sort_order = 520
	max_available_positions = 1
	skill = learning

	court_position_asset = {
		animation = personality_zealous
		background = "gfx/interface/illustrations/event_scenes/temple.dds"
	}

	opinion = {
		value = 20
	}

	aptitude_level_breakpoints = { 20 40 60 80 }

	aptitude = {
		value = 0

		# Learning dominates, as the office demands. Other skills
		# contribute only marginally -- a charming administrator does
		# not make a good posek.
		add = {
			value = learning
			multiply = 1.5
			max = 90
			desc = court_position_skill_learning
		}
		add = {
			value = diplomacy
			multiply = 0.25
			max = 20
			desc = court_position_skill_diplomacy
		}

		if = {
			limit = { has_trait = theologian }
			add = 20
			desc = court_position_theologian_trait
		}
		if = {
			limit = { has_trait = scholar }
			add = 15
			desc = court_position_scholar_trait
		}
		if = {
			limit = { has_trait = just }
			add = 10
			desc = court_position_just_trait
		}
		if = {
			limit = { has_trait = arbitrary }
			add = -20
			desc = court_position_arbitrary_trait
		}

		add = court_position_aptitude_high_penalty_value
	}

	is_shown = {
		custom_tooltip = {
			text = kehillah_government_tt
			government_has_flag = government_is_kehillah
		}
	}

	valid_position = {
		custom_tooltip = {
			text = kehillah_government_tt
			government_has_flag = government_is_kehillah
		}
		custom_tooltip = {
			text = kehillah_requires_beit_midrash_tt
			domicile ?= { has_domicile_parameter = kehillah_unlocks_chief_rabbi }
		}
	}

	is_shown_character = {
		scope:employee = {
			is_incapable = no
		}
	}

	valid_character = {
		# The community's rabbi must be of the community's faith. This
		# is not a preference, it is what the office does.
		custom_tooltip = {
			text = kehillah_officer_same_faith_tt
			scope:employee = {
				faith = scope:employer.faith
			}
		}
		scope:employee = {
			is_adult = yes
		}
	}

	salary = {
		round = no
		gold = {
			value = 0.5
		}
	}

	scaling_employer_modifiers = {
		terrible = {
			learning = 1
			monthly_piety = 0.1
		}
		poor = {
			learning = 1
			monthly_piety = 0.15
		}
		average = {
			learning = 2
			monthly_piety = 0.25
			domicile_monthly_influence_add = 0.25
		}
		good = {
			learning = 3
			monthly_piety = 0.35
			domicile_monthly_influence_add = 0.5
		}
		excellent = {
			learning = 4
			monthly_piety = 0.5
			domicile_monthly_influence_add = 0.75
		}
	}

	modifier = {
		monthly_learning_lifestyle_xp_gain_mult = 0.1
	}

	candidate_score = {
		value = @kehillah_officer_will_do
		add = court_position_candidate_score_base_value
		add = court_position_candidate_aptitude_value
	}
}


# =====================================================================
# TREASURER (GABBAI) -- Stewardship. Gated on the Countinghouse.
# =====================================================================

treasurer_kehillah_position = {
	sort_order = 519
	max_available_positions = 1
	skill = stewardship

	court_position_asset = {
		animation = personality_greedy
		background = "gfx/interface/illustrations/event_scenes/market_west.dds"
	}

	opinion = {
		value = 15
	}

	aptitude_level_breakpoints = { 20 40 60 80 }

	aptitude = {
		value = 0

		add = {
			value = stewardship
			multiply = 1.5
			max = 90
			desc = court_position_skill_stewardship
		}
		add = {
			value = learning
			multiply = 0.25
			max = 20
			desc = court_position_skill_learning
		}

		if = {
			limit = { has_trait = diligent }
			add = 15
			desc = court_position_diligent_trait
		}
		if = {
			limit = { has_trait = just }
			add = 10
			desc = court_position_just_trait
		}
		# A treasurer who steals is worse than no treasurer. Weighted
		# heavily so the AI recommendation does not cheerfully suggest
		# one.
		if = {
			limit = { has_trait = greedy }
			add = -25
			desc = court_position_greedy_trait
		}
		if = {
			limit = { has_trait = lazy }
			add = -15
			desc = court_position_lazy_trait
		}

		add = court_position_aptitude_high_penalty_value
	}

	is_shown = {
		custom_tooltip = {
			text = kehillah_government_tt
			government_has_flag = government_is_kehillah
		}
	}

	valid_position = {
		custom_tooltip = {
			text = kehillah_government_tt
			government_has_flag = government_is_kehillah
		}
		custom_tooltip = {
			text = kehillah_requires_countinghouse_tt
			domicile ?= { has_domicile_parameter = kehillah_unlocks_treasurer }
		}
	}

	is_shown_character = {
		scope:employee = {
			is_incapable = no
		}
	}

	valid_character = {
		custom_tooltip = {
			text = kehillah_officer_same_faith_tt
			scope:employee = {
				faith = scope:employer.faith
			}
		}
		scope:employee = {
			is_adult = yes
		}
	}

	salary = {
		round = no
		gold = {
			value = 0.4
		}
	}

	scaling_employer_modifiers = {
		terrible = {
			stewardship = 1
		}
		poor = {
			stewardship = 1
			domicile_monthly_gold_add = 0.1
		}
		average = {
			stewardship = 2
			domicile_monthly_gold_add = 0.25
		}
		good = {
			stewardship = 3
			domicile_monthly_gold_add = 0.4
			domicile_monthly_gold_mult = 0.05
		}
		excellent = {
			stewardship = 4
			domicile_monthly_gold_add = 0.6
			domicile_monthly_gold_mult = 0.1
		}
	}

	modifier = {
		monthly_stewardship_lifestyle_xp_gain_mult = 0.1
	}

	candidate_score = {
		value = @kehillah_officer_will_do
		add = court_position_candidate_score_base_value
		add = court_position_candidate_aptitude_value
	}
}


# =====================================================================
# SHTADLAN -- Diplomacy. Gated on the Shtadlan's Chambers.
#
# The community's advocate to outside authority. In v1 this office is
# what makes the "negotiate" resolution available on protection events
# (see events/kehillah_protection_events.txt), which is the concrete
# form of spec section 3b's promise that "whichever officer you have
# invested in should visibly matter here".
#
# This is also the office that grows most in Phase 4, when host-realm
# charters become a real system rather than flavor.
# =====================================================================

shtadlan_kehillah_position = {
	sort_order = 518
	max_available_positions = 1
	skill = diplomacy

	court_position_asset = {
		animation = personality_honorable
		background = "gfx/interface/illustrations/event_scenes/ep3_city_gate.dds"
	}

	opinion = {
		value = 15
	}

	aptitude_level_breakpoints = { 20 40 60 80 }

	aptitude = {
		value = 0

		add = {
			value = diplomacy
			multiply = 1.5
			max = 90
			desc = court_position_skill_diplomacy
		}
		add = {
			value = intrigue
			multiply = 0.25
			max = 20
			desc = court_position_skill_intrigue
		}

		if = {
			limit = { has_trait = patient }
			add = 15
			desc = court_position_patient_trait
		}
		if = {
			limit = { has_trait = gregarious }
			add = 15
			desc = court_position_gregarious_trait
		}
		if = {
			limit = { has_trait = shy }
			add = -15
			desc = court_position_shy_trait
		}
		if = {
			limit = { has_trait = wrathful }
			add = -20
			desc = court_position_wrathful_trait
		}

		add = court_position_aptitude_high_penalty_value
	}

	is_shown = {
		custom_tooltip = {
			text = kehillah_government_tt
			government_has_flag = government_is_kehillah
		}
	}

	valid_position = {
		custom_tooltip = {
			text = kehillah_government_tt
			government_has_flag = government_is_kehillah
		}
		custom_tooltip = {
			text = kehillah_requires_shtadlan_chambers_tt
			domicile ?= { has_domicile_parameter = kehillah_unlocks_shtadlan }
		}
	}

	is_shown_character = {
		scope:employee = {
			is_incapable = no
		}
	}

	valid_character = {
		# Deliberately NOT faith-gated, unlike the other two. A
		# shtadlan's entire job is dealing with outside authority, and
		# an advocate with standing among the host population is
		# historically exactly the sort of person a community would
		# want speaking for it. Leaving this open is a small piece of
		# the Phase 4 host relationship arriving early.
		scope:employee = {
			is_adult = yes
		}
	}

	salary = {
		round = no
		gold = {
			value = 0.5
		}
	}

	scaling_employer_modifiers = {
		terrible = {
			diplomacy = 1
		}
		poor = {
			diplomacy = 1
			monthly_prestige = 0.1
		}
		average = {
			diplomacy = 2
			monthly_prestige = 0.2
			domicile_monthly_influence_add = 0.25
		}
		good = {
			diplomacy = 3
			monthly_prestige = 0.3
			domicile_monthly_influence_add = 0.4
		}
		excellent = {
			diplomacy = 4
			monthly_prestige = 0.4
			domicile_monthly_influence_add = 0.6
		}
	}

	modifier = {
		monthly_diplomacy_lifestyle_xp_gain_mult = 0.1
	}

	candidate_score = {
		value = @kehillah_officer_will_do
		add = court_position_candidate_score_base_value
		add = court_position_candidate_aptitude_value
	}
}
