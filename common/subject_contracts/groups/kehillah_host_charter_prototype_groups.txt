# =====================================================================
# V16 regional Host Charter prototypes.
#
# These groups are selected only when a fresh charter is established for
# a Christian or Muslim host. The shipped V15 group stays intact for
# existing contracts and remains the safe fallback for every other host
# tradition until it has its own researched package. This is deliberately
# *selection on creation*, not a migration: a saved V15 charter is never
# reinterpreted or silently rewritten.
#
# The policy envelope controls each row's valid/default levels. Every
# community under the same host reads the same host-primary-title policy,
# while retaining its own actual negotiated contract afterwards.
# =====================================================================

kehillah_christian_host_charter_prototype = {
	is_tributary = yes
	tributary_can_break_free = { always = no }
	tributary_heir_succession = yes
	suzerain_heir_succession = yes
	contracts = {
		kehillah_christian_quarter_building_rights
		kehillah_christian_community_security
		kehillah_christian_jurisdiction
		kehillah_christian_text_censorship
	}
}

kehillah_muslim_host_charter_prototype = {
	is_tributary = yes
	tributary_can_break_free = { always = no }
	tributary_heir_succession = yes
	suzerain_heir_succession = yes
	contracts = {
		kehillah_muslim_quarter_building_rights
		kehillah_muslim_community_security
		kehillah_muslim_jurisdiction
		kehillah_muslim_market_access
	}
}
