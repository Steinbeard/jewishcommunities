# =====================================================================
# THE COMMUNITY LIBRARY -- the one value the GUI reads. ADDED 2026-09-15
# (docs/spec/v13-domicile-library-panel.md, live pass 3).
#
# Scope: a CHARACTER (the domicile's owner). 1 if their quarter has a
# Beit Midrash of any tier, else 0. The library panel is visible exactly
# when this is 1 -- the Beit Midrash IS the library; no building, no
# library view. Read from gui/kehillah_domicile_library.gui as
# Character.MakeScope.ScriptValue( 'kehillah_has_beit_midrash_value' ),
# the idiom the strip and roster already use for script values.
#
# has_domicile_building_or_higher is a DOMICILE-scope trigger, so it
# sits inside domicile ?= { } -- the exact shape kehillah_bd_beit_
# midrash_value (common/script_values/kehillah_breakdown_values.txt)
# uses, after the baseline bug v12 section 8 records.
# =====================================================================

kehillah_has_beit_midrash_value = {
	value = 0
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_beit_midrash_01 } }
		value = 1
	}
}

# =====================================================================
# DONATING A WORK. ADDED 2026-09-18 at user request ("an inventory
# management system... click a donate book option for some piety").
# The first inventory ACTION on a held work (gui/kehillah_domicile_
# library.gui's per-row donate button -> scripted_gui kehillah_lib_
# donate_work -> event kehillah_library.0001). Both are read by the
# event's option effects and its tooltips.
#
# Calibration: acquiring a work (kehillah_acquire_torah_work_decision)
# costs medium_gold_value on a two-year cooldown and yields a RANDOM
# missing work, and every study effect needs the work present -- so a
# donation trades real study material for piety and cannot be farmed
# faster than one work per two years. Medium for a community that
# lacks the work (the copy stays in the world, in a library the player
# can later travel to); minor for the "send it abroad" fallback when
# every registered community already holds it.
# =====================================================================

kehillah_donate_work_piety = medium_piety_gain
kehillah_donate_work_abroad_piety = minor_piety_gain
