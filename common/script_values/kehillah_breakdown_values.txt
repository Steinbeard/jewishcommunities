# =====================================================================
# Kehillah pillar BREAKDOWN values -- one per contributor to a baseline.
#
# ADDED 2026-09-15 (v12 section 8, user request: hover a score and see
# what makes it up). Backs the KehillahBd* customizable-loc lines
# (common/customizable_localization/kehillah_breakdown_custom_loc.txt)
# that the map view's per-score tooltips are assembled from.
#
# THESE ARE THE SOURCE OF TRUTH. As of the 2026-09-15 refactor (v12
# spec section 8), kehillah_prosperity_baseline_value / kehillah_
# stability_baseline_value / kehillah_greatness_baseline_value
# (kehillah_script_values.txt) are plain sums of the values below, so a
# building's or office's contribution is defined exactly once, here, and
# the baseline and the tooltip explaining it can never disagree. Change
# a number here and both move together. (This file was briefly a copy
# of the baseline terms while that file was being edited by a parallel
# session; that duplication is gone.)
#
# Scope: the community's LEADER (character), same as the baselines --
# has_domicile_building_or_higher, employs_court_position and the skill
# reads all live on the character.
# =====================================================================

kehillah_bd_countinghouse_value = {
	value = 0
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_countinghouse_01 } }
		add = 80
	}
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_countinghouse_02 } }
		add = 70
	}
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_countinghouse_03 } }
		add = 90
	}
}

kehillah_bd_market_stalls_value = {
	value = 0
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_market_stalls_01 } }
		add = 50
	}
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_market_stalls_02 } }
		add = 50
	}
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_market_stalls_03 } }
		add = 70
	}
}

kehillah_bd_workshops_value = {
	value = 0
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_workshops_01 } }
		add = 40
	}
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_workshops_02 } }
		add = 40
	}
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_workshops_03 } }
		add = 60
	}
}

kehillah_bd_slaughterhouse_value = {
	value = 0
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_slaughterhouse_01 } }
		add = 25
	}
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_slaughterhouse_02 } }
		add = 35
	}
}

kehillah_bd_hekdesh_value = {
	value = 0
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_hekdesh_01 } }
		add = 60
	}
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_hekdesh_02 } }
		add = 60
	}
}

kehillah_bd_synagogue_value = {
	value = 0
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_synagogue_01 } }
		add = 40
	}
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_synagogue_02 } }
		add = 30
	}
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_synagogue_03 } }
		add = 40
	}
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_synagogue_04 } }
		add = 40
	}
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_synagogue_05 } }
		add = 50
	}
}

kehillah_bd_beit_midrash_value = {
	value = 0
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_beit_midrash_01 } }
		add = 50
	}
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_beit_midrash_02 } }
		add = 40
	}
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_beit_midrash_03 } }
		add = 60
	}
}

kehillah_bd_sofer_workshop_value = {
	value = 0
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_sofer_workshop_01 } }
		add = 20
	}
	if = {
		limit = { domicile ?= { has_domicile_building_or_higher = kehillah_sofer_workshop_02 } }
		add = 30
	}
}

# Leader and officer skill terms, weights from kehillah_script_values.txt
# (kehillah_baseline_leader_stat_weight / _officer_stat_weight).
kehillah_bd_leader_stewardship_value = {
	value = stewardship
	multiply = kehillah_baseline_leader_stat_weight
}
kehillah_bd_leader_learning_value = {
	value = learning
	multiply = kehillah_baseline_leader_stat_weight
}
kehillah_bd_treasurer_value = {
	value = 0
	if = {
		limit = { employs_court_position = treasurer_kehillah_position }
		court_position:treasurer_kehillah_position = {
			add = {
				value = stewardship
				multiply = kehillah_baseline_officer_stat_weight
			}
		}
	}
}
kehillah_bd_chief_rabbi_value = {
	value = 0
	if = {
		limit = { employs_court_position = chief_rabbi_kehillah_position }
		court_position:chief_rabbi_kehillah_position = {
			add = {
				value = learning
				multiply = kehillah_baseline_officer_stat_weight
			}
		}
	}
}

# Stability's two office terms. The flat amounts are what the tooltip
# line shows; the _in_post twins are what the baseline sums, zero when
# the seat is empty.
kehillah_bd_shtadlan_value = 90
kehillah_bd_gabbai_tzedakah_value = 40
kehillah_bd_shtadlan_in_post_value = {
	value = 0
	if = {
		limit = { kehillah_has_shtadlan_trigger = yes }
		add = kehillah_bd_shtadlan_value
	}
}
kehillah_bd_gabbai_tzedakah_in_post_value = {
	value = 0
	if = {
		limit = { employs_court_position = gabbai_tzedakah_kehillah_position }
		add = kehillah_bd_gabbai_tzedakah_value
	}
}

# ---------------------------------------------------------------------
# Host Charter contributors (V16 first implementation slice).
#
# The contract flag, not the host's future settlement-policy forecast,
# is the source of these values. This is deliberately the legacy V15
# charter's moneylending/walled-quarter shape: V16 replaces those rows
# only after its contract migration spike is live-proven. Keeping this
# adapter small gives that spike one clear question: does an actual
# tributary-contract flag read back into a quarterly pillar baseline?
#
# Scope: the community leader / tributary. The numbers are modest next
# to a building tier (and zero at V15's default rungs) so a charter is a
# meaningful structural condition, not a replacement for development of
# the Jewish Quarter. V16 §5 makes these source values reusable by the
# eventual projected-charter outlook as well as the actual charter.
# ---------------------------------------------------------------------
kehillah_bd_charter_economic_privileges_value = {
	value = 0
	if = {
		limit = { vassal_contract_has_flag = kehillah_moneylending_full_granted }
		add = 45
	}
	else_if = {
		limit = { vassal_contract_has_flag = kehillah_moneylending_pawnbroking_granted }
		add = 20
	}
}

kehillah_bd_charter_quarter_protection_value = {
	value = 0
	if = {
		limit = { vassal_contract_has_flag = kehillah_quarter_walled_granted }
		add = 25
	}
}

# ---------------------------------------------------------------------
# V16 regional-charter contributors. These consume the deferred title
# cache of actual contract flags, never the host realm's policy forecast:
# policy determines the offer when a charter is formed, and these values
# determine the resulting pillar baseline afterwards. A right is defined
# once here, then used by both the baseline and its named tooltip line.
# ---------------------------------------------------------------------
kehillah_bd_charter_construction_value = {
	value = 0
	if = {
		limit = { kehillah_cached_charter_construction_free_trigger = yes }
		add = 15
	}
	else_if = {
		limit = { kehillah_cached_charter_construction_permission_trigger = yes }
		add = 5
	}
	else_if = {
		limit = { kehillah_cached_charter_construction_forbidden_trigger = yes }
		add = -10
	}
}

kehillah_bd_charter_security_value = {
	value = 0
	if = {
		limit = { kehillah_cached_charter_security_strong_trigger = yes }
		add = 25
	}
	else_if = {
		limit = { kehillah_cached_charter_security_watch_trigger = yes }
		add = 10
	}
}

kehillah_bd_charter_jurisdiction_stability_value = {
	value = 0
	if = {
		limit = { kehillah_cached_charter_jurisdiction_bet_din_trigger = yes }
		add = 22
	}
	else_if = {
		limit = {
			kehillah_cached_christian_charter_trigger = yes
			kehillah_cached_charter_jurisdiction_royal_appeal_trigger = yes
		}
		add = 12
	}
	else_if = {
		limit = {
			kehillah_cached_muslim_charter_trigger = yes
			kehillah_cached_charter_jurisdiction_communal_arbitration_trigger = yes
		}
		add = 10
	}
}

kehillah_bd_charter_jurisdiction_greatness_value = {
	value = 0
	if = {
		limit = { kehillah_cached_charter_jurisdiction_bet_din_trigger = yes }
		add = 20
	}
	else_if = {
		limit = {
			kehillah_cached_christian_charter_trigger = yes
			kehillah_cached_charter_jurisdiction_royal_appeal_trigger = yes
		}
		add = 4
	}
	else_if = {
		limit = {
			kehillah_cached_muslim_charter_trigger = yes
			kehillah_cached_charter_jurisdiction_communal_arbitration_trigger = yes
		}
		add = 4
	}
}

kehillah_bd_charter_text_study_value = {
	value = 0
	if = {
		limit = { kehillah_cached_charter_text_unrestricted_trigger = yes }
		add = 25
	}
	else_if = {
		limit = { kehillah_cached_charter_text_licensed_trigger = yes }
		add = 5
	}
	else_if = {
		limit = { kehillah_cached_charter_text_proscribed_trigger = yes }
		add = -30
	}
}

kehillah_bd_charter_market_access_value = {
	value = 0
	if = {
		limit = { kehillah_cached_charter_market_chartered_trigger = yes }
		add = 40
	}
	else_if = {
		limit = { kehillah_cached_charter_market_regional_trigger = yes }
		add = 20
	}
	else_if = {
		limit = { kehillah_cached_charter_market_local_trigger = yes }
		add = 5
	}
}

# Local urban opportunity is a settlement condition, not a charter
# right. Development is the best engine-owned proxy for the barony's
# market depth, services, and institutional capacity. It deliberately
# uses broad bands rather than a raw linear value: a community should
# care about settling in a town instead of a hamlet, without county
# development overwhelming its own buildings and offices.
#
# Scope: community leader. County scope is reached through the fixed
# domicile location, just as host-charter resolution does elsewhere.
# The two values are separated so the tooltip and baseline retain a
# single source of truth for each pillar.
kehillah_bd_urban_opportunity_prosperity_value = {
	value = -5
	if = {
		limit = { domicile.domicile_location.county = { development_level >= 25 } }
		add = 20 # +15: major city
	}
	else_if = {
		limit = { domicile.domicile_location.county = { development_level >= 15 } }
		add = 13 # +8: established town
	}
	else_if = {
		limit = { domicile.domicile_location.county = { development_level >= 5 } }
		add = 10 # +5: market settlement
	}
}

kehillah_bd_urban_opportunity_stability_value = {
	value = -3
	if = {
		limit = { domicile.domicile_location.county = { development_level >= 25 } }
		add = 9 # +6: major city
	}
	else_if = {
		limit = { domicile.domicile_location.county = { development_level >= 15 } }
		add = 6 # +3: established town
	}
	else_if = {
		limit = { domicile.domicile_location.county = { development_level >= 5 } }
		add = 3 # 0: market settlement
	}
}

# Nearby-community Goldilocks contribution. The count is cached on the
# title by kehillah_refresh_settlement_network_effect, not recomputed
# during baseline evaluation. A nearby peer network is a Stability
# resource; isolation hurts, but an overly dense local cluster provides
# less marginal support rather than an ever-growing bonus.
kehillah_bd_settlement_network_value = {
	value = -5
	if = {
		limit = {
			primary_title = {
				has_variable = kehillah_nearby_community_count
				var:kehillah_nearby_community_count >= 5
			}
		}
		add = 5
	}
	else_if = {
		limit = {
			primary_title = {
				has_variable = kehillah_nearby_community_count
				var:kehillah_nearby_community_count >= 3
			}
		}
		add = 12
	}
	else_if = {
		limit = {
			primary_title = {
				has_variable = kehillah_nearby_community_count
				var:kehillah_nearby_community_count >= 1
			}
		}
		add = 20
	}
}

# ---------------------------------------------------------------------
# Drift this season: what BASELINE CONVERGENCE will add next quarterly
# tick -- (baseline - current) * kehillah_convergence_rate_value, the
# exact expression kehillah_quarterly_pillars_effect applies. Signed;
# the _abs twins exist because the tooltip says "rising by" / "falling
# by" a positive number rather than printing a minus sign.
# ---------------------------------------------------------------------
kehillah_bd_prosperity_drift_value = {
	value = kehillah_prosperity_baseline_value
	if = {
		limit = { primary_title = { has_variable = kehillah_var_prosperity } }
		subtract = primary_title.var:kehillah_var_prosperity
	}
	multiply = kehillah_convergence_rate_value
}
kehillah_bd_prosperity_drift_abs_value = {
	value = kehillah_bd_prosperity_drift_value
	if = {
		limit = { kehillah_bd_prosperity_drift_value < 0 }
		multiply = -1
	}
}

kehillah_bd_stability_drift_value = {
	value = kehillah_stability_baseline_value
	if = {
		limit = { primary_title = { has_variable = kehillah_var_stability } }
		subtract = primary_title.var:kehillah_var_stability
	}
	multiply = kehillah_convergence_rate_value
}
kehillah_bd_stability_drift_abs_value = {
	value = kehillah_bd_stability_drift_value
	if = {
		limit = { kehillah_bd_stability_drift_value < 0 }
		multiply = -1
	}
}

kehillah_bd_greatness_drift_value = {
	value = kehillah_greatness_baseline_value
	if = {
		limit = { primary_title = { has_variable = kehillah_var_greatness } }
		subtract = primary_title.var:kehillah_var_greatness
	}
	multiply = kehillah_convergence_rate_value
}
kehillah_bd_greatness_drift_abs_value = {
	value = kehillah_bd_greatness_drift_value
	if = {
		limit = { kehillah_bd_greatness_drift_value < 0 }
		multiply = -1
	}
}
